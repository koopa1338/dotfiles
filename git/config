[user]
    name = koopa1338
    email = sinner1991@gmail.com
[alias]
	a = add
	aa = a --all
	ap = a -p
	au = a -u
	aup = a -up
	br = branch
	bra = br -a
	cl = clone
	cm = commit
	cma = cm --amend
    cmf = cm --fixup
	cmi = cm --interactive
	cmm = cm -m
	co = checkout
	dis = co --
	f = fetch
	fa = f --all
	fp = f -p
	i = init
	ll = log --graph --pretty='format:%C(red)%d%C(reset) %C(yellow)%h%C(reset) %ar %C(green)%aN%C(reset) %s'
	ls = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset'
	lsa = !git ls --all
	mb = !git checkout --no-track -B `git rev-parse --abbrev-ref HEAD`
	out = !git --no-pager ls --branches --simplify-by-decoration --not --remotes
	p = push
	pf = p --force-with-lease
	pr = p --rebase
	rb = rebase
	rba = rb --abort
	rbc = rb --continue
	rbi = rb --interactive
	rbia = rb --interactive --autosquash
	rbs = rb --skip
	remotes = remote -v
	rs = reset
	st = status -sb
	sta = stash
	uncm = reset --mixed HEAD~
	unstage = reset HEAD --
    be = br --edit-description
    d = diff
    dc = d --cached
    ds = d --staged
    dst = d --stats
    incoming = remote update --prune; git log ..@{upstream}
    pl = pull
    pln = pl --rebase --no-autostash
    p = push
    pf = p --force-with-lease
    pub = push -u origin $(git branch-name)
    stal = sta list
    stap = sta pop
    tags = tag -n1 --list
    unpub = !git push origin :$(git branch-name)


[color]
    diff = auto
    status = auto
    branch = auto
    ui = true

[push]
    default = simple
    followTags = true
[diff]
	tool = nvimdiff
    colorMoved = default
[difftool "nvimdiff"]
	cmd = nvim -d $LOCAL $REMOTE
[core]
    pager = delta
	autocrlf = input
    askPass = true
    excludesFile = ~/.config/git/ignore
[credential]
	helper = store
[rebase]
    autoStash = true
[interactive]
    diffFilter = delta --color-only
[delta]
    features = decorations
[delta "decorations"]
    file-style = bold 7 box
    file-decoration-style = none
    minus-style = normal 1
    minus-non-emph-style = normal 1
    minus-empty-line-marker-style = normal 1
    minus-emph-style = normal 9
    line-numbers-minus-style = 1
    side-by-side = false
    line-numbers = true
    syntax-theme = base16
    plus-style = normal 5
    plus-non-emph-style = normal 5
    plus-empty-line-marker-style = normal 5
    plus-emph-style = normal 13
    line-numbers-plus-style = 5

[includeIf "gitdir:~/dev/"]
    path = ~/dotfiles/git/work

[includeIf "gitdir:~/homeoffice/"]
    path = ~/dotfiles/git/work
